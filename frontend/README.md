# RiskGuard AI — Frontend

Vite + React + Tailwind dashboard for the RiskGuard AI risk manager.
See the [root README](../README.md) for full setup, architecture, and deploy instructions.

Quick start:

```bash
npm install
cp .env.example .env   # set VITE_API_URL to your backend
npm run dev
```
